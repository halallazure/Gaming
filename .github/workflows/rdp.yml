name: Ultimate-Gaming-VM-V41-MAX-FPS
on: workflow_dispatch

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: 1. Unlock Maximum Performance & 4K Driver
        shell: pwsh
        run: |
          # Create folders
          New-Item -Path "C:\GamingVM\Backup" -ItemType Directory -Force
          New-Item -Path "C:\GamingVM\Startup" -ItemType Directory -Force
          
          # FORCE High Performance Power Plan
          powercfg /setactive 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c
          
          # Install Visual C++ (Required for Sunshine)
          curl.exe -L -o vc_redist.x64.exe https://aka.ms/vs/17/release/vc_redist.x64.exe
          Start-Process ./vc_redist.x64.exe -ArgumentList "/install /quiet /norestart" -Wait
          
          # INSTALL 4K VIRTUAL DISPLAY (Unlocks Sunshine Encoders)
          curl -L -o VDD.zip https://github.com/itsmikethetech/Virtual-Display-Driver/releases/download/2.3/IddSampleDriver.zip
          Expand-Archive VDD.zip -DestinationPath C:\VDD
          certutil -addstore -f "TrustedPublisher" C:\VDD\IddSampleDriver.cer
          pnputil /add-driver C:\VDD\IddSampleDriver.inf /install

      - name: 2. Install Sunshine & 60 FPS Hack
        shell: pwsh
        run: |
          # Install Sunshine
          curl -L -o sunshine.exe https://github.com/LizardByte/Sunshine/releases/latest/download/sunshine-windows-installer.exe
          Start-Process -FilePath ".\sunshine.exe" -ArgumentList "/S" -Wait
          
          # Unlock 60 FPS Registry
          reg add "HKLM\System\CurrentControlSet\Control\Terminal Server\WinStations" /v DWMFRAMEINTERVAL /t REG_DWORD /d 15 /f

      - name: 3. Restore Data & Boost Priority
        shell: pwsh
        env:
          M_USER: ${{ secrets.MEGA_EMAIL }}
          M_PASS: ${{ secrets.MEGA_PASSWORD }}
        run: |
          $m = "${env:LOCALAPPDATA}\MEGAcmd"
          Start-Process "$m\MEGAcmdServer.exe" -WindowStyle Hidden
          Start-Sleep -Seconds 30
          & "$m\mega-login.bat" "$env:M_USER" "$env:M_PASS"
          & "$m\mega-get.bat" /GamingVM/Data.7z C:\
          
          if (Test-Path "C:\Data.7z") {
            7z x C:\Data.7z -oC:\GamingVM\ -y
            
            # Launch apps and force them to use "REALTIME" priority for 60 FPS
            Get-ChildItem "C:\GamingVM\Startup" | ForEach-Object {
                $p = Start-Process $_.FullName -PassThru
                $p.PriorityClass = [System.Diagnostics.ProcessPriorityClass]::RealTime
            }
          }
          if (Test-Path "C:\GamingVM\Backup\System.reg") { reg import "C:\GamingVM\Backup\System.reg" }

      - name: 4. AnyDesk Connection
        shell: pwsh
        run: |
          echo "GitHub@2026" | & "C:\AnyDesk\AnyDesk.exe" --set-password
          # The rest of your AnyDesk script...
