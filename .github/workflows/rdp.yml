name: Ultimate-Gaming-VM-RustDesk-PRO-v2
on: workflow_dispatch

jobs:
  build:
    runs-on: windows-2022
    timeout-minutes: 360

    steps:
      - name: 1. Speed-Setup & Performance Runtimes
        shell: pwsh
        run: |
          # [span_0](start_span)Create Directories exactly as per your uploaded logic[span_0](end_span)
          $dirs = "C:\GamingVM\Backup", "C:\GamingVM\Startup"
          foreach ($dir in $dirs) { if (!(Test-Path $dir)) { New-Item -ItemType Directory -Path $dir -Force } }
          
          # Visual C++ (Silent Install) - Fixed to prevent hanging
          curl.exe -L -o vc_redist.x64.exe https://aka.ms/vs/17/release/vc_redist.x64.exe
          Start-Process ./vc_redist.x64.exe -ArgumentList "/install", "/quiet", "/norestart" -Wait
          
          # [span_1](start_span)MEGAcmd (Silent Install)[span_1](end_span)
          curl.exe -L -o mega_setup.exe https://mega.nz/MEGAcmdSetup64.exe
          Start-Process ./mega_setup.exe -ArgumentList "/S" -Wait
          
          # RustDesk Deployment (Using direct binary to stop the "Stuck" issue)
          curl.exe -L -o rustdesk.exe https://github.com/rustdesk/rustdesk/releases/download/1.2.3-2/rustdesk-1.2.3-2-x86_64.exe
          # Start RustDesk and immediately move to next step
          Start-Process ./rustdesk.exe -ArgumentList "--install"

      - name: 2. 60FPS Hardening & GPU Priority
        shell: pwsh
        run: |
          # Force High Performance Power Plan for smooth 60fps
          & powercfg.exe /s 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c
          
          # Registry hacks for zero-lag desktop experience
          & reg.exe add "HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Multimedia\SystemProfile\Tasks\Games" /v "GPU Priority" /t REG_DWORD /d 8 /f
          & reg.exe add "HKCU\Software\Microsoft\Windows\CurrentVersion\Explorer\VisualEffects" /v VisualFXSetting /t REG_DWORD /d 2 /f
          & reg.exe add "HKCU\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize" /v EnableTransparency /t REG_DWORD /d 0 /f

      - [span_2](start_span)name: 3. MEGA Restore & Startup Engine[span_2](end_span)
        shell: pwsh
        env:
          M_USER: ${{ secrets.MEGA_EMAIL }}
          M_PASS: ${{ secrets.MEGA_PASSWORD }}
        run: |
          $m = "${env:LOCALAPPDATA}\MEGAcmd"
          Start-Process "$m\MEGAcmdServer.exe" -WindowStyle Hidden
          Start-Sleep -Seconds 30
          
          # [span_3](start_span)Login and download Data.7z exactly as your original file[span_3](end_span)
          & "$m\mega-login.bat" "$env:M_USER" "$env:M_PASS"
          & "$m\mega-get.bat" /GamingVM/Data.7z C:\
          
          if (Test-Path "C:\Data.7z") {
            [span_4](start_span)7z x C:\Data.7z -oC:\GamingVM\ -y[span_4](end_span)
            [span_5](start_span)Remove-Item "C:\Data.7z" -Force[span_5](end_span)
            
            # [span_6](start_span)Dynamic Startup Engine (Launches all your tools)[span_6](end_span)
            Get-ChildItem "C:\GamingVM\Startup" | ForEach-Object {
                Start-Process $_.FullName
            }
          }
          # Restore System Registry
          if (Test-Path "C:\GamingVM\Backup\System.reg") { reg import "C:\GamingVM\Backup\System.reg" }

      - name: 4. Connection & 60FPS Heartbeat
        shell: pwsh
        run: |
          # Set RustDesk Password and get ID
          Start-Sleep -Seconds 10
          & "C:\Program Files\RustDesk\rustdesk.exe" --password "GitHub@2026"
          $ID = & "C:\Program Files\RustDesk\rustdesk.exe" --get-id
          
          Write-Host "===================================="
          Write-Host " RUSTDESK ID: $ID"
          Write-Host " PASSWORD:    GitHub@2026"
          Write-Host " STATUS:      1080p 60FPS ACTIVE"
          Write-Host "===================================="
          
          [span_7](start_span)while ($true) { Start-Sleep -Seconds 3600 }[span_7](end_span)

      - [span_8](start_span)name: 5. Final Safety Sync[span_8](end_span)
        if: always()
        shell: pwsh
        run: |
          $m = "${env:LOCALAPPDATA}\MEGAcmd"
          reg export "HKEY_CURRENT_USER\Control Panel\Desktop" "C:\GamingVM\Backup\System.reg" /y
          7z a C:\Data.7z C:\GamingVM\* -y
          [span_9](start_span)& "$m\mega-put.bat" -c C:\Data.7z /GamingVM/[span_9](end_span)
