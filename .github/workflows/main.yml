    - name: Setup AnyDesk (Force ID)
      shell: pwsh
      run: |
        $ProgressPreference = 'SilentlyContinue'
        # 1. Download AnyDesk
        curl.exe -L -o anydesk.exe https://download.anydesk.com/AnyDesk.exe
        
        # 2. Install as a service (Critical for ID generation)
        .\anydesk.exe --install "C:\Program Files (x86)\AnyDesk" --start-with-win --silent
        
        # 3. Set the password
        echo "Gaming123" | & "C:\Program Files (x86)\AnyDesk\AnyDesk.exe" --set-password
        
        # 4. Wait for the service to communicate with AnyDesk servers
        Write-Host "Waking up AnyDesk service..."
        Start-Sleep -Seconds 45
        
        # 5. Extract ID directly from the installed service path
        $anyid = & "C:\Program Files (x86)\AnyDesk\AnyDesk.exe" --get-id
        Write-Host "-------------------------------------------"
        Write-Host "YOUR ANYDESK ID IS: $anyid"
        Write-Host "-------------------------------------------"
